# Dependencies
node_modules/

# Builds
dist/
build/
*.tsbuildinfo

# Lokale Laufzeit-Daten (Spielstände, SQLite, LLM-Cache).
# WICHTIG: nur den Runtime-Ordner des Servers ignorieren — NICHT den
# Quellcode-Ordner shared/src/data/ (Präzedenzfälle & Glossar). Ein
# unverankertes `data/` würde jeden gleichnamigen Ordner erfassen.
/server/data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Umgebungsvariablen (API-Keys!)
.env
.env.local

# Sonstiges
coverage/
.DS_Store
*.log
