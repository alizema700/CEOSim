{
  "name": "boardroom",
  "private": true,
  "version": "0.1.0",
  "description": "Boardroom — realistischer CEO-Simulator mit KI-Charakteren und deterministischer Unternehmens-Simulation",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run typecheck && npm run build -w client",
    "test": "npm run test -w shared",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.12"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.6.3"
  }
}
